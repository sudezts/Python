#Things you should not do when assigning a name to a variable:
#cannot start with a number
#cannot contain spaces
#Cannot contain symbols such as "*/(
#cannot use reserved names such as input

number=24
print(number)

number=25
print(number)

number+=1 #number=number+1
print(number)
